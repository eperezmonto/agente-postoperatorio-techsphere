# Configuracion del agente. Copie este archivo a .env si desea sobreescribir.
# El proyecto funciona sin .env: todos los valores tienen defecto.

OLLAMA_URL=http://localhost:11434
MODELO_LLM=llama3.2:3b
DB_PATH=datos.db
PUERTO=8000

# Este proyecto NO usa claves de API. El modelo corre localmente via Ollama.
